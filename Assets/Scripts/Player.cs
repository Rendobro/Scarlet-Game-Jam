using UnityEngine;

public class Player : MonoBehaviour
{
    int health = 5;
    int speed = 5;
    int damage = 1;

    // List<Buff> playerBuffs = Buff.newList();


    // Note: in c# if theres no "public" or "private" for methods like start and update it 
    // just means it defaults to private
    void Start()
    {
        
    }
    void Update()
    {
        
    }
}
